name: app
description: Json placeholder usage and simple mvvm usage

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  http: ^1.1.0
  lazy_load_scrollview: 1.3.0

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
